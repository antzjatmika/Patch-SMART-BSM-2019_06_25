@model IEnumerable<MVCSmartClient01.Models.trxFileDataPekerjaan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdRekanan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FileName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BlobPekerjaan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdRekanan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FileName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BlobPekerjaan)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.IdDataPekerjaan }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.IdDataPekerjaan })
        </td>
    </tr>
}

</table>
