@model IEnumerable<MVCSmartClient01.Models.mstTypeOfRegion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdRegion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdSupervisor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RegionDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdRegion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdSupervisor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegionDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item. }) |
            @Html.ActionLink("Delete", "Delete", new { id = item. })
        </td>
    </tr>
}

</table>
