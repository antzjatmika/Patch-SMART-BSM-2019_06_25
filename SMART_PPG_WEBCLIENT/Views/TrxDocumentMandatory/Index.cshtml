@model IEnumerable<MVCSmartClient01.Models.trxDocumentMandatory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdTypeOfRekanan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdTypeOfDocument)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsMandatory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdTypeOfRekanan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdTypeOfDocument)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsMandatory)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.IdMandatoryDocument }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.IdMandatoryDocument })
        </td>
    </tr>
}

</table>
